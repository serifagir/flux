import 'package:flutter/material.dart';

class SessionNameIndicator extends StatelessWidget {
  const SessionNameIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("flux");
  }
}
